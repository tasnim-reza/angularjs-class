angular.module('app').controller('registrationCtrl',function($scope, schedule, instructors, registration, currentUser, students, policies, global, $) {
  // kill me now....


});


window.global = 'something';

angular.module('app').value('$',$);
angular.module('app').value('_',_);